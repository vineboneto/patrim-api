generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:768879@localhost:5432/patrim"
}

model Sector {
  id   Int    @id @default(autoincrement())
  name String
}
